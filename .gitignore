# Salesforce
.sfdx
/src.orig
/src
.sf/
.forceignore
.sf
.codegenie

# Python
*.pyc
__pycache__

# Editors
*.sublime-project
*.sublime-workspace
.vscode
.idea
.project
.settings

# Misc
.DS_Store

# AWS Credentials
.aws
lambda.zip

# Lambda pip-installed deps (build with: pip install -r requirements.txt -t .)
# Playwright is 100MB+ and not used in Lambda (screenshots skipped)
lambda/playwright/
lambda/playwright-*.dist-info/
lambda/requests/
lambda/requests-*.dist-info/
lambda/beautifulsoup4/
lambda/beautifulsoup4-*.dist-info/
lambda/soupsieve/
lambda/soupsieve-*.dist-info/
lambda/certifi/
lambda/urllib3/
lambda/charset_normalizer/
lambda/idna/
lambda/*.dist-info/*.so
